fn main() {
    let cond1: bool = true;
    let cond2: bool = false;
    let cond3: bool = cond1 || cond2;
    let cond4: bool = cond1 && cond2;
    let cond5: bool = cond1 || cond2 || cond3;
    exit(0);
}