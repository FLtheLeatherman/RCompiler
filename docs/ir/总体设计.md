入口：ir generator，是 ast_visitor，根据每个 ast node 调用 ir builder 等生成 ir.

采用 type-value 的设计，每个 value 均有一个对应的 (llvm) type.

ir builder 用于直接输出一些语句的字符串形式。

ir builder 还要维护临时的寄存器编号，basic blocks 的编号。

ir generator 里头需要记载：全局的 function 和 变量；局部的、随着 scope 变化的变量。