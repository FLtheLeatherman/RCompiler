#pragma once

#include <regex>

enum class Token {

    // tokens
    kIdentifier,
    kComments,
    kIntergerLiterals,
    kCharacter,
    kString,
    kRawString,
    kCString,
    kRawCString,

    
};